/**
 * Phase 3 stub. Default compile input must exclude `low` / `none` (LNK-6).
 * Until this package implements compile, filter via
 * `defaultCompileEpisodes` / `isCompileEligible` from `@graft/linking`
 * (or equivalent artifact-side filter in pipeline/CLI).
 */
export const PKG = "@graft/compile" as const;
